# Todo List App

A responsive Todo List web application built using HTML, CSS, and JavaScript. Tasks can be added, marked as completed, deleted, and are saved using localStorage so they persist after page refresh.

